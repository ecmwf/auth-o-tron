# Test-only RSA keys

These RSA private keys and matching public keys are checked-in fixtures used only by unit and integration tests. The 512-bit and 1024-bit fixtures verify that both server signing and client verification reject weak keys. All are public, insecure, and must never be used by a deployment.
